# O `react-i18next` declara peer de typescript ^5 e o projeto esta no 6. E peer OPCIONAL — nao
# afeta o runtime — mas o npm 7+ trata como erro e trava a instalacao. Fica registrado aqui para
# ninguem precisar redescobrir a flag.
legacy-peer-deps=true
